<!-- views/dashboard.handlebars -->
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Dashboard</title>
</head>
<body>
  <h1>Dashboard</h1>
  <ul>
    {{#each blogs}}
      <li>
        <a href="/blogs/{{this.id}}">{{this.title}}</a>
      </li>
    {{/each}}
  </ul>
</body>
</html>